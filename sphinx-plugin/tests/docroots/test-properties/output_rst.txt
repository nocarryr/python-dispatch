**class the_module.TheClass(*args, **kwargs)**

   Docstring for TheClass

   Look at ``foo``, ``bar`` and
   ``baz``

   ``Property foo``

      Docstring for foo

      ``foo`` is a ``pydispatch.Property`` object and defaults to ``False``.

   ``DictProperty bar``

      Docstring for bar

      ``bar`` is a ``pydispatch.DictProperty`` object.

   ``ListProperty baz``

      Docstring for baz

      ``baz`` is a ``pydispatch.ListProperty`` object.

**class the_module.TheOtherClass(*args, **kwargs)**

   Docstring for TheOtherClass

   Look at ``foo``, ``bar`` and
   ``baz``

   ``Property foo: int``

      Docstring for foo

      ``foo`` is a ``pydispatch.Property`` object and defaults to ``0``.

   ``DictProperty bar: Dict[str, the_module.TheClass]``

      Docstring for bar

      ``bar`` is a ``pydispatch.DictProperty`` object.

   ``ListProperty baz: List[str]``

      Docstring for baz

      ``baz`` is a ``pydispatch.ListProperty`` object.
